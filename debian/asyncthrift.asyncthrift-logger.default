# Defaults for asyncthrift-logger initscript
# sourced by /etc/init.d/asyncthrift-logger
# installed at /etc/default/asyncthrift-logger by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
ASYNCTHRIFT_CONFIG_DIR="/etc/asyncthrift"
ASYNCTHRIFT_USER="asyncthrift"
